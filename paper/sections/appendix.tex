% ---------------------------------------------------------------------------
% APPENDIX -- ADDED S5 WAVE 4, AND THE REASON IS A MEASUREMENT, NOT A PREFERENCE.
%
% Wave 3's prose pass met its 24-line target (body 165 -> 141 typeset lines) and the
% body STILL overran, because four floats -- two tables and the two figures wave 2
% added -- do not pack into four pages.  Agent G2 had honoured rulebook rule 6 by
% moving numbers out of running prose INTO the Table 1 and Figure 2 captions, but
% caption text costs exactly what body text costs; the load was relocated into floats
% rather than reduced, and floats pack worse.  With LaTeX's default float glue this
% put Table 1 and Figure 2 on the SAME page and left that page eight body lines.
%
% E-values' call, fetched headlessly on 2026-08-20 and quoted verbatim, reads:
%   "Short papers up to 4 pages, EXCLUDING REFERENCES AND OPTIONAL APPENDICES."
% So an appendix is explicitly permitted and does not count against the ceiling.
% Rule 6 allows a fact to move "to a table, an appendix, or a figure caption"; this
% is that move, to the one destination that is free.  NOTHING IS DELETED HERE: every
% number below was printed in the paper before this wave, and each is still traceable
% to results/ by the same field paths audit sub-session A recorded.
%
% =========================================================================================
% S15 SUB-SESSION D (2026-08-24): THE PARAGRAPH ABOVE DESCRIBES A DESTINATION THAT IS NO
% LONGER FREE, AND EVERY PLACEMENT DECISION IN THIS FILE WAS RE-TAKEN ON THAT BASIS.
%
% TS-LIMITS' call reads "4 to 7 pages plus references" and states NO appendix exclusion
% anywhere -- verified fresh 2026-08-24 by a targeted full-text search of the call
% (research/S15/A-venue-verification.json, field a1_findings.3_appendix_policy: "NOT
% MENTIONED, at all, anywhere in the call ... Assume NO appendix allowance").  So the
% 4-7 page range covers the WHOLE document, body and appendices together.  There is no
% page-exempt destination any more, and "move it to the appendix" is no longer a way to buy
% body pages -- it is only a way to tell a reader that something is secondary.
%
% The header text above is KEPT rather than deleted because it is the accurate record of why
% this file exists, and a later session that flips the venue back to E-values needs it.  What
% is deleted is the sentence that used to follow it -- "TS-LIMITS allows 4-7 BODY pages, so
% under that option this material could return to the body.  It is kept here because the
% tighter of the two live options governs" -- because its premise ("body pages") is now known
% to be wrong and a later session could act on it.
%
% WHAT STAYED AND WHY, decided element by element rather than reflexively:
%   Table 1 (eleven arms)          -> BODY.  Split table de-duplicated; see below.
%   Figure 2 (the cliff)           -> BODY.  It is the headline claim's picture.
%   Figure 3 (19 runs, 5 settings) -> BODY.  It is the class claim's picture.
%   Table~\ref{tab:stress}         -> STAYS.  Body prose already states all three
%                                     configurations it exhibits, with their numbers.
%   The five prose paragraphs here -> STAY.  Convention analysis, provenance, and measurement
%                                     commentary whose load-bearing conclusions the body
%                                     states independently.   [REVERSED BY D2; SEE BELOW.]
%   "The four vocabularies"        -> MERGED into Section~\ref{sec:related}; see below.
% Record, with the reasoning and the full before/after cross-reference map:
% research/S15/D-restructuring.json.
% =========================================================================================
%
% =========================================================================================
% S15 SUB-SESSION D2 (2026-08-25): THE FIVE PROSE PARAGRAPHS ARE DELETED, ON EXPLICIT
% OPERATOR AUTHORISATION, TO CLOSE THE PAGE BUDGET.  THIS REVERSES D's "STAY" ABOVE.
%
% D measured the paper at 9.77 counted pages (total pages less the bibliography) against
% TS-LIMITS' 7-page whole-document ceiling, recovered 0.91pp of pure duplication, and then
% DECLINED to cut content, recording the two largest cuttable blocks for the operator
% (research/S15/D-restructuring.json, "residual_overage").  The operator authorised both.
% This file's half is rank 2: the five paragraphs, 1.04pp.
%
% DELETED HERE, and each is quoted in full in research/S15/D2-budget-closure.json:
%   "The primary regime."                       -- convention analysis.  The adversary it
%       describes is fully specified in the BODY, in sections/setup.tex's Harness paragraph
%       ("It plays $s_t = clip(qtilde_t + eps, +-b/2)$, eps = 1e-9, against the DEPLOYED
%       qtilde_t ... firing the strict indicator"), which also states that every number in
%       Table~\ref{tab:forfeit} is measured against it.  What is lost is the 44-of-44
%       cell-identity check between the two counting conventions and the eleven mirror
%       readings, not the specification.
%   "Excursions at $T = 10^4$ ..."              -- a fitted law the paper itself disowns
%       ("a fit to those five arms, not a derived bound").
%   "Realised miscoverage at the covering settings of Figure~\ref{fig:settings}."
%   "What survives of the measurement ..."      -- its two claim-bearing conclusions are
%       stated independently in the body: the tangent integrator sitting inside the set is
%       the tightness paragraph of Section~\ref{sec:forfeit}, and that condition~(4) is not
%       what fails is that section's opening paragraph.  Its endpoint evidence is
%       Table~\ref{tab:stress}'s first two rows, which stay.
%   "Where the rate stops being a tell."        -- an illustration of a claim the body makes
%       in the clause immediately before its pointer, off Table~\ref{tab:stress} row 12.
%
% NOTHING ELSE IN THIS FILE IS TOUCHED.  Table~\ref{tab:stress} keeps all thirteen rows, its
% caption, its column heads and its \label; the appendix heading and \label{app:support} stay.
% Three body sentences that pointed AT the deleted paragraphs are repointed, not left to
% dangle: Table~\ref{tab:forfeit}'s caption, Figure~\ref{fig:boundary}'s caption, and the
% mirror-boundary paragraph -- all in sections/forfeit.tex, each marked there.
% THIS IS A DELIBERATE DEVIATION from this session's standing "restore, don't cut" rule and
% from this project's convention that deleting measurement description is a serious act.  It
% is taken on explicit operator authorisation and on nothing else.
% =========================================================================================
% ---------------------------------------------------------------------------


\appendix

\section{Supporting measurements}
\label{app:support}


% =========================================================================================
% S16 SUB-SESSION C (2026-08-26): TABLE~\ref{tab:forfeit}, FIGURE~\ref{fig:boundary} AND
% FIGURE~\ref{fig:settings} ARE BACK HERE, AT THE HEAD OF THIS APPENDIX, IN THE ORDER THEY
% HELD BEFORE S15 SUB-SESSION D: eleven-arm table, the cliff, the five settings, then
% Table~\ref{tab:stress}.  THIS IS A REVERSAL OF A VENUE-SPECIFIC MOVE, NOT A NEW DECISION.
%
% THE PREMISE THAT MOVED THEM OUT IS GONE.  S15 sub-session D promoted all three into
% Section~\ref{sec:forfeit} on one stated ground: TS-LIMITS' "4 to 7 pages plus references"
% states no appendix exclusion, so "an appendix page and a body page cost the same" and
% "relocation now moves zero pages".  The paper is submitted to E-values again, whose call
% reads "Short papers up to 4 pages, EXCLUDING REFERENCES AND OPTIONAL APPENDICES" -- the
% sentence quoted at the top of this file, re-verified 2026-08-26 by a byte-identical page
% fetch with the same sha256 as S4's 2026-08-20 record (research/S16/A-venue-reconfirmation.json).
% Under that call the destination this file was built for is free again, D's promotion buys
% nothing and costs three floats' worth of a four-page body, and the placement S10 sub-session D
% and S6 recorded -- which the paper then held at exactly 4 body pages from S10 through S14 --
% is restored.
%
% WHAT MOVED, AND WHAT DID NOT.  All three floats move WHOLE: graphic or tabular, caption word
% for word, \label, float specifier, and the provenance comments that sit inside them.  Every
% post-S15 repair travels unchanged -- S15 sub-session D2's caption repointing at
% Section~\ref{sec:setup} on Table~\ref{tab:forfeit}, and its repointing of
% Figure~\ref{fig:boundary}'s last clause at the exact-threshold column (which is why that
% caption is NOT self-referential now that the figure sits inside the appendix it used to name).
% The five deleted Appendix~A prose paragraphs are NOT restored: that was an authorised content
% cut, it is out of this sub-session's scope, and nothing moved here points at them.
% NOT MOVED, and unchanged in the body: the four-boundary theorem and both proof directions,
% (\ref{eq:boundaries}), (\ref{eq:closed}) and the closed-form paragraph, both degenerate cases,
% the hedges, and the folded-in e-value diagnostic paragraph.
%
% NUMBERING.  Document order gives the body's four-row table Table 1, the eleven-arm table
% Table 2 and the boundary stress Table 3; figures keep 1 schematic / 2 cliff / 3 settings,
% since both figures were already in this order and no float crosses another.  No table or
% figure number is hardcoded anywhere in the paper, so every \ref resolves without edit.
% Record: research/S16/C-appendix-reversal.json.
% =========================================================================================
% S10 SUB-SESSION D: TABLE 1 MOVED HERE FROM sections/forfeit.tex, CAPTION AND ALL ELEVEN
% ARM ROWS CELL FOR CELL UNCHANGED.  Placed at the head of Appendix~A so document order
% preserves the numbering.  Reason: the E-values 4-page ceiling, which excludes appendices.
% S16 SUB-SESSION C: that reason is live again, and the caption differs from S10's in one
% respect only -- it points at Section~\ref{sec:setup} for the adversary rather than at this
% appendix's deleted primary-regime paragraph, which is S15 sub-session D2's repair, kept.
\begin{table}[!ht]
\centering
\scriptsize
\setlength{\tabcolsep}{4.5pt}
% S5 wave 4: arraystretch 0.95.  Eleven data rows at 0.95 give back one typeset line.
% This is row LEADING only -- no font change, no row removed, no cell altered.
\renewcommand{\arraystretch}{0.95}
% S6 SUB-SESSION B: CAPTION REWRITTEN TO DESCRIBE ONLY.  Two clauses left, and neither
% was deleted:
%   "so no covering arm is withheld" was a REBUTTAL, not a description.  The fact it
%     defended -- that every arm run appears -- is still the caption's first sentence.
%   "five exceed the bound at T = 10^6" was a CONCLUSION about the data, and it existed
%     NOWHERE ELSE in the paper.  It moved into the retention paragraph, where Table 1 is
%     first discussed.  Independently recounted from the table's own ratio column: 4.25,
%     42.10, 3.58, 3.34 and 60,747 exceed 1, so five is right.
\caption{Placement~A: a one-scalar readout on the \emph{completed} threshold, under one
deterministic adversary, on one pass to $T = 10^6$; all eleven arms run are listed.
Proposition~2's bound is $13.2061$ at $T = 2{\times}10^5$ and $14.8155$ at $T = 10^6$;
``ratio'' is $\max_t|E_t|$ over it, and ``Sat.'' the fraction of rounds on which~(4)
delivers its full $\pm b$. Every number is measured against the adversary
Section~\ref{sec:setup} specifies.}
% S15 SUB-SESSION D2 (2026-08-25): THE CAPTION'S LAST SENTENCE IS REPOINTED, NOT SOFTENED.  It
% read "Every number is the PRIMARY regime's, defined in Appendix~\ref{app:support}, which also
% gives the T = 10^4 excursions and the fitted excursion law."  All three of those targets --
% the regime definition, the T = 10^4 excursions and the fitted law -- were in the two Appendix
% A paragraphs the operator authorised deleting this sub-session, so the sentence pointed at
% content that no longer exists.  The regime IS still specified, in the body, in
% Section~\ref{sec:setup}'s Harness paragraph, which is where the caption now points.  No
% number and no claim about the table changes.
\label{tab:forfeit}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
Readout on the completed threshold & miscov.\ $2{\times}10^5$ & miscov.\ $10^6$ &
$\max_t|E_t|$ $10^6$ & ratio & Sat.\ $10^6$ & travel $10^6$ \\
\midrule
none (control)                & $0.100035$ & $0.100007$ & $7.80$        & $0.53$     & $0.0000$ & $28{,}311$ \\
partial adj.\ $w = 0.5$       & $0.100030$ & $0.100007$ & $7.70$        & $0.52$     & $0.0000$ & $18{,}113$ \\
partial adj.\ $w = 0.9$       & $0.100030$ & $0.100007$ & $7.50$        & $0.51$     & $0.0000$ & $4{,}081$ \\
partial adj.\ $w = 0.99$      & $0.100040$ & $0.100006$ & $63.00$       & $4.25$     & $0.0007$ & $1{,}023$ \\
partial adj.\ $w = 0.999$     & $0.100035$ & $\mathbf{0.100004}$ & $623.70$      & $42.10$    & $0.0097$ & $202$ \\
growing time constant $p=0.5$ & $0.100030$ & $0.100006$ & $12.60$       & $0.85$     & $0.0000$ & $330$ \\
growing time constant $p=0.9$ & $0.100030$ & $0.100024$ & $53.00$       & $3.58$     & $0.3181$ & $16$ \\
running mean                  & $0.099940$ & $0.100010$ & $49.50$       & $3.34$     & $0.3647$ & $9$ \\
dead band $\tau = 0.5$        & $0.100035$ & $0.100005$ & $10.60$       & $0.72$     & $0.0000$ & $2{,}045$ \\
dead band $\tau = 0.9$        & $0.100060$ & $0.100012$ & $13.20$       & $0.89$     & $0.0038$ & $1{,}051$ \\
dead band $\tau = 1.5$        & $\mathbf{1.000000}$ & $\mathbf{1.000000}$ & $900{,}000$ & $60{,}747$ & $1.0000$ & $0.5$ \\
\bottomrule
\end{tabular}
\end{table}

% S10 SUB-SESSION D: FIGURE 2 MOVED HERE FROM sections/forfeit.tex, GRAPHIC AND CAPTION
% UNCHANGED.  It is placed BEFORE Figure~\ref{fig:settings} so that the document order, and
% therefore the numbering, is exactly what it was: Figure 1 the placements schematic in the
% body, Figure 2 the cliff, Figure 3 the five settings.  Its rebuild history is in
% sections/forfeit.tex's header, where the figure used to sit.
% S16 SUB-SESSION C (2026-08-26): RESTORED TO THIS POSITION after S15 sub-session D's
% venue-specific promotion into the body; see the block at the head of this appendix.  The
% caption's last clause is S15 sub-session D2's wording, not S10's -- it names the run's
% exact-threshold column instead of the deleted primary-regime paragraph -- so it reads
% correctly from inside this appendix and no cross-reference in it is self-referential.
\begin{figure}[!ht]
  \centering
  \includegraphics[width=\textwidth]{../figures/figure1_boundary.pdf}
  % S17 SUB-SESSION C (2026-08-26): CAPTION COMPRESSED 6 SENTENCES/150 WORDS -> 3 SENTENCES,
  % DESCRIPTION-ONLY, for the E-values caption-length pass.  Sentences 1+2 merged (setup +
  % panel (a), joined by a colon) and the grey-band clause folded into panel (a)'s sentence,
  % since both are panel-(a) visual elements; the symlog-distortion disclosure survives as its
  % own clause inside panel (b)'s sentence, unabridged ("visual spacing ... is not proportional
  % to true distance in tau") because it is a legibility-honesty disclosure, not filler.  The
  % unlabelled-minor-tick values (0.95, 1.05) are KEPT, folded parenthetically into panel (b)'s
  % sentence -- nothing is lost, only re-punctuated.  CUT ENTIRELY, not relocated: the closing
  % sentence identifying the mirror-adversary reading with this run's exact-threshold column.
  % That fact is ALREADY STATED, near-verbatim, in the body: sections/forfeit.tex's "The mirror
  % boundary is already measured, on this paper's own grid" paragraph reads "The tau-cliff run's
  % exact-threshold column, read against the mirror adversary $s_t \equiv -b/2$
  % (Figure~\ref{fig:boundary}, App.~\ref{app:support}), locates $\tau^{\star-} = 1$ at the null
  % scorecaster, closed on the failing side, exactly where (\ref{eq:boundaries}) puts it." So
  % cutting the caption's copy loses nothing; the stronger, argument-bearing site is the body
  % paragraph, which already points back at this figure. Record: research/S17/C-caption-compression.json.
  \caption{Realised miscoverage against dead-band width $\tau$ at the null scorecaster,
  $\hat{q} \equiv 0$ with a saturator of level $b$, under the \emph{specified} adversary,
  where the upward boundary $\tau^{\star+}$ of (\ref{eq:boundaries}) is $1$: \textbf{(a)} the
  full sweep, filled markers to $T = 10^{6}$ and the four widest bands, drawn open, to $T =
  2{\times}10^{5}$, blue covering and orange forfeiting, with the dashed rule at $\tau^{\star+}$
  and the grey band spanning the widest covering width to the narrowest failing one.
  \textbf{(b)} The same setting at $T = 10^{5}$ over eleven measured widths, placed by $\tau -
  \tau^{\star+}$ on a symlog axis (linear zone $|\tau - \tau^{\star+}| \le 10^{-3}$; unlabelled
  minor ticks at $\tau = 0.95$ and $1.05$) that magnifies the neighbourhood of $\tau^{\star+}$,
  so visual spacing there is not proportional to true distance in $\tau$. Figure~\ref{fig:settings}
  gives the other four $(r_t,\hat{q})$ settings.}
  \label{fig:boundary}
\end{figure}

% S6 SUB-SESSION A: FIGURE 3, NEW.  It carries the five-setting comparison that S5's
% Figure 2 panel (a) held as a five-series overlay in one continuous panel.
% THE AXIS PRINCIPLE, which is why this is a separate figure and not another panel:
% the (r_t, qhat) SETTING is a categorical variable and gets a categorical axis (five
% rows); tau is CONTINUOUS and keeps a log axis.  The overlay had to place five series
% and five tau* markers on one continuous pair of axes, which is what produced four
% nested markers at (tau = 0.5, miscoverage ~ 0.1) and a legend that overwrote two of
% the tau* rules.  Rows do not collide.
% All 19 runs appear, and the two tau* values that fall off the axis (0 and unbounded) are
% drawn as arrows rather than dropped.
% Built by the same generator as Figure~\ref{fig:boundary}: src/make_figure1.py.
% S6's own note said "IT LIVES IN THE APPENDIX because E-values excludes appendices from the
% 4-page ceiling and the body is exactly full; nothing about the figure requires the body."
% S15 sub-session D dropped that clause as obsolete under TS-LIMITS.  S16 SUB-SESSION C
% (2026-08-26) RESTORES IT, because the venue it was written against is the venue again --
% see the block at the head of this appendix.  It is quoted rather than paraphrased so that a
% later session flipping venues can read the trigger without reconstructing it.
\begin{figure}[!ht]
  \centering
  \includegraphics[width=\textwidth]{../figures/figure3_settings.pdf}
  % S6 SUB-SESSION B: written to the SAME rule as the other two captions -- describe the
  % axes, the encoding and the measurement conditions, and argue nothing.  Four sentences.
  % S17 SUB-SESSION C (2026-08-26): COMPRESSED 5 SENTENCES/118 WORDS -> 3 SENTENCES,
  % description-only, for the E-values caption-length pass.  NOTHING IS CUT: sentences 1+2
  % (axes/rows + marker encoding) are joined by a colon, and sentences 3+4 (the per-row rule/
  % tinted band + the two off-axis arrow cases) are joined by a semicolon; the closing
  % mirror-boundary scope sentence stands alone, unabridged, because it is not stated anywhere
  % else in the paper and this is the only figure it disambiguates against ("not drawn here").
  % Record: research/S17/C-caption-compression.json.
  \caption{All $19$ dead-band runs at the five admissible $(r_t,\hat{q})$ settings, rows
  ordered by $\tau^{\star}$ on the shared horizontal $\tau$ axis: blue markers cover at
  $\approx \alpha$ and orange markers return $1.000000$, filled to $T = 10^{6}$ and open to
  $T = 2{\times}10^{5}$, all under the \emph{specified} adversary. The rule on each row is
  that setting's upward boundary $\tau^{\star+} = \sup_x r_t(x) + \sup_t \hat{q}_t - b/2$,
  printed at the right, with the tinted band $\tau \le \tau^{\star+}$; two rules fall off the
  axis and are drawn as arrows, $\tau^{\star+} = 0$ at $\hat{q} \equiv -b/2$ and
  $\tau^{\star+}$ unbounded under \citeauthor{angelopoulos2023pid}'s tangent integrator. The
  mirror boundary $\tau^{\star-}$ of (\ref{eq:boundaries}) is not drawn here and differs from
  $\tau^{\star+}$ at the two non-null scorecasters.}
  \label{fig:settings}
\end{figure}


% S10 SUB-SESSION D: TABLE 2 MOVED HERE FROM sections/forfeit.tex, WHOLE AND CELL FOR CELL
% UNCHANGED (caption, column heads, all thirteen rows, the \label, the S9 provenance comment
% below).  Its stated reason was "the 4-page ceiling and nothing else".
% S15 SUB-SESSION D RE-DECIDED THIS ONE FROM SCRATCH AND KEPT IT HERE.  The old reason is void
% under TS-LIMITS, so keeping it could not be inherited; it had to be re-earned, and it is.
% Section~\ref{sec:forfeit} states, in prose and with the numbers, all three configurations
% this table exhibits: the unreachable supremum ("reads tau*+ = 19, and fails at tau = 1.001"),
% the varying-qhat split ("reads tau*+ = 2 against sigma+ = 1"), and -- via
% sections/limitations.tex's open-band disclosure -- the power-of-two rows.  A reader following
% the argument therefore needs none of the thirteen rows; a referee auditing it wants all
% thirteen, with the "proved window" column and its three empty-set entries.  That asymmetry is
% what an appendix is for, and it is the same verdict the S15 brief's operator guessed for this
% table -- confirmed on the argument, not assumed.  Placed after the Figure~\ref{fig:settings}
% paragraph so that Appendix~A reads measurement-then-boundary-stress; [!ht] rather than [tb]
% because the appendix has no page-top competition and floating it later would strand it.
\begin{table}[!ht]
\centering
\scriptsize
\setlength{\tabcolsep}{3.1pt}
\renewcommand{\arraystretch}{0.95}
% S9 SUB-SESSION C: NEW.  Every cell is
% results/boundary-stress-20260822T103716Z-cd208b98.json, field `rows', selected by
% (config, tau, adversary, T).  Produced by src/boundary_stress.py, which imports
% src/forfeit.py unmodified and validates its transcription against
% results/forfeit-20260820T063132Z-83747c45.json (19 exact checks) before running.
% Nothing here is read off a figure and nothing is rounded by hand.
% S17 SUB-SESSION C (2026-08-26): COMPRESSED 5 SENTENCES/82 WORDS -> 3 SENTENCES,
% description-only, for the E-values caption-length pass.  NOTHING IS CUT: sentence 1 (the
% configurations + adversary count) is joined to sentence 2 (the "proved window" column gloss
% and its scope disclosure) by a semicolon; sentence 3 ($\emptyset$'s meaning) is joined to
% sentence 4 (the horizon exception) by "and"; the closing sentence on the dead-band-free last
% row stands alone, unabridged.  The "neither direction is claimed" clause is kept as a
% description of what the "proved window" column means, not as an argued conclusion.
% Record: research/S17/C-caption-compression.json.
% S17 SUB-SESSION F (2026-08-26): GRAMMAR ONLY, ZERO CONTENT CHANGE.  C's merge of the old
% sentences 2 and 3 left the last clause as a dangling absolute -- "with rows built on a supremum
% the dynamics cannot reach or never attain sitting there" -- in which the participle "sitting"
% sits twelve words from its subject, and the definite article the pre-C wording carried ("the
% rows", i.e. these rows of this table) was dropped, making the reference indefinite.  Restored to
% a finite verb with a relative pronoun: "which is where the rows built on a supremum the dynamics
% cannot reach or never attain sit."  Same three sentences, same facts, same scope, three
% characters longer; re-measured after the edit: 9 pages, body still ends p.4, References still
% first on p.5.  Record: research/S17/F-final-read.json.
\caption{Legal $(r_t,\hat{q})$ configurations on which the printed $\tau^{\star}$ over-certifies,
each under two legal adversaries; ``proved window'' is where (\ref{eq:boundaries}) certifies
retention against every legal adversary, and between it and the failure boundaries
$\tau^{\star\pm}$ neither direction is claimed, which is where the rows built on a supremum the
dynamics cannot reach or never attain sit. Here $\emptyset$ marks a configuration for which no
width is certified, $\tau = 0$ included, and $T = 10^5$ except the two power-of-two rows, at
$T = 10^6$. The last row carries no dead band at all.}
\label{tab:stress}
\begin{tabular}{@{}lrrrrrrr@{}}
\toprule
 & & & & \multicolumn{2}{c}{$s_t = \tilde{q}_t+\varepsilon$} &
 \multicolumn{2}{c}{$s_t \equiv -b/2$} \\
\cmidrule(lr){5-6}\cmidrule(lr){7-8}
$(r_t,\hat{q})$ configuration & $\tau$ & printed $\tau^{\star}$ & proved window &
miscov. & $\max_t|E_t|$ & miscov. & $\max_t|E_t|$ \\
\midrule
$\hat{q} \equiv +0.4$, level $b$        & $0.599$ & $1.4$ & $\tau < 0.6$ & $0.100010$ & $7.20$ & $0.099890$ & $11.60$ \\
$\hat{q} \equiv +0.4$, level $b$        & $0.8$   & $1.4$ & $\tau < 0.6$ & $0.100050$ & $8.70$ & $\mathbf{0.000000}$ & $10{,}000$ \\
$\sup_x r_t = 10b$, off the reach       & $1.001$ & $19$  & $\tau < 1$   & $\mathbf{1.000000}$ & $90{,}000$ & $0.000000$ & $10{,}000$ \\
$\sup_x r_t$ unbounded, off the reach   & $1.5$   & $\infty$ & $\tau < 1$ & $\mathbf{1.000000}$ & $90{,}000$ & $0.000000$ & $10{,}000$ \\
$A^{+} = 4b$, $A^{-} = -b$              & $1.5$   & $7$   & $\tau < 1$   & $0.100020$ & $4.30$ & $\mathbf{0.000000}$ & $10{,}000$ \\
level $4b$, $\hat{q} \equiv 0$          & $6.999$ & $7$   & $\tau < 7$   & $0.100070$ & $12.20$ & $0.100050$ & $11.60$ \\
level $4b$, $\hat{q} \equiv 0$          & $7.0$   & $7$   & $\tau < 7$   & $0.100090$ & $12.20$ & $\mathbf{0.000000}$ & $10{,}000$ \\
$\sup_x r_t = 2b$, unattained           & $2.99$  & $3$   & $\tau < 1$   & $0.100320$ & $211.10$ & $0.098010$ & $210.50$ \\
$\sup_x r_t = 2b$, unattained           & $3.0$   & $3$   & $\tau < 1$   & $\mathbf{1.000000}$ & $90{,}000$ & $0.000000$ & $10{,}000$ \\
$\sup_x r_t = 2b$, attained             & $3.0$   & $3$   & $\tau < 3$   & $0.100000$ & $12.20$ & $\mathbf{0.000000}$ & $10{,}000$ \\
$\hat{q}_t = b/2$ on powers of $2$      & $1.0$   & $2$   & $\emptyset$    & $0.100009$ & $14.30$ & $\mathbf{0.000000}$ & $100{,}000$ \\
$\hat{q}_t = b/2$ on powers of $2$      & $1.9$   & $2$   & $\emptyset$    & $\mathbf{0.110960}$ & $84{,}745.70$ & $0.000000$ & $100{,}000$ \\
$\hat{q} \equiv +b/2$, no band          & $0$     & $2$   & $\emptyset$    & $0.100000$ & $0.90$ & $\mathbf{0.000000}$ & $10{,}000$ \\
\bottomrule
\end{tabular}
\end{table}

% =========================================================================================
% S16 SUB-SESSION D (2026-08-26): NEW APPENDIX SECTION, RECEIVING THE DERIVATION HALF OF
% Section~\ref{sec:forfeit}'s "The boundary read as a bet." paragraph.  E-values' ceiling is
% "4 pages, excluding references and optional appendices" and the body stood one full page over
% it; that paragraph was the densest block of secondary material in the body.  Everything below
% is RELOCATED, not rewritten: the martingale, Ville's inequality, the three citations, the
% \mathcal{F}_{t-1} gloss, lambda's range, the pre-registration caveat, the five-interval verdict
% and the e-value/e-process disclosure are the same text the body carried, reordered only so the
% construction precedes the verdict it produces.  The two hedges the operator's brief protects in
% the BODY ("It adds a diagnostic and a reading, not a result" and "no result above assumes that
% null or is certified by that test") stay in the body pointer AND the second is restated here in
% its own scope, so neither section can be read alone and over-claim.
%
% S18 SUB-SESSION B (2026-08-27): VALIDITY FIX.  The clause S15 sub-session J restored as F2 --
% "lambda is fixed in [-1/(1-alpha), 1/alpha], and its sign is chosen after the fact, not
% pre-registered" -- did not qualify the verdict it sat next to; it INVALIDATED it.  A test whose
% betting direction is picked once the realised path is known is not a test martingale at all, so
% Ville's inequality does not apply and "anytime-valid ... at the 5% level" was not a claim the
% construction supported.  Verified against the primary sources this paragraph already cites,
% not against the audit's characterisation of them:
%   Ramdas, Gruenwald, Vovk & Shafer, arXiv:2210.01948v2 (the ramdas2023savi preprint), Sec. 5.2,
%     eq. (24), states the identical process family prod_i (1 + lambda_i (X_i - mu)) over the
%     identical null class (bounded, E[X_i | F_{i-1}] = mu, which contains conditional
%     Bernoulli(alpha)) and says of it: "The lambda_i are naturally interpreted as bets on the
%     X_i; they must be predictable because a bet on X_i must be made before seeing X_i."
%     "Predictable" is defined in its Sec. 1.1 as F_{t-1}-measurable.  Its Sec. 2.5 states Ville's
%     inequality with the hypothesis "if M is a test martingale for P", and Sec. 2.2 defines that
%     as nonnegative, M_0 = 1, and E_P[M_t | F_{t-1}] = M_{t-1}.  A post-hoc lambda cannot be
%     pulled out of that conditional expectation, so the martingale property fails outright.
%   Sec. 2.9 of the same paper is the closure property the fix rests on: "Test (super)martingales
%     and e-processes can also be mixed, yielding mixture (super)martingales or e-processes", and
%     for the e-value form, "even if E_1 and E_2 are dependent (for example, calculated in
%     different ways using the same data)" -- which is exactly the M(+1)/M(-1) case, since both
%     are computed from one path.  Sec. 5.1 gives the continuous two-sided version (a centred
%     Gaussian mixing distribution over lambda); the two-atom mixture used here is its discrete
%     special case.  Vovk & Wang (vovk2021evalues), Sec. 1 and Thm. 3.2, and Shafer & Vovk
%     (shafer2019gtfpf), whose Ville's-theorem statement ramdas2023savi Sec. 2.5 restates, agree.
%   The 1/0.05 = 20 threshold is that literature's own default, stated in ramdas2023savi Sec. 2.6:
%     "simply reject the null (and stop) the first time the process reaches or exceeds 1/alpha".
%     It is always valid, and conservative rather than unimprovable, so printing it is safe.
% WHY THE MIXTURE (option b) AND NOT A PRE-REGISTERED PAIR OF ONE-SIDED TESTS (option a).  A pair
% of one-sided tests each read at 5% is two tests, and the paragraph's second claim says "the SAME
% test cannot reject inside the edge"; keeping one object keeps that sentence meaning what it
% says, and avoids a multiplicity caveat the paragraph has no room to state properly.  The mixture
% is also the honest answer to the defect: it needs no direction in advance at all, rather than
% asserting a pre-registration that this paper's runs did not actually perform.
% WHAT THE CLAUSE'S SUBSTANCE COST.  Nothing is quietly dropped.  S15's F3 (lambda's range, a
% correctness fix, since it is what makes M nonnegative) is KEPT, now attached to both signs.  F2
% (the pre-registration caveat) is REMOVED rather than softened, because under the mixture there
% is no after-the-fact choice left for it to disclose; the honesty it was protecting is now
% supplied by the construction instead of by a warning about the construction.  The concession
% side of the paragraph is untouched word for word: "for that imposed null alone", "signed",
% "neither an e-value nor an e-process", "no result in Section 4 assumes that null or is certified
% by that test".
% THE NUMBERS, ALL RECOMPUTED, NONE CARRIED OVER.  alpha = 0.1 (sections/setup.tex, "The harness
% fixes alpha = 0.1 and b = 2"); threshold 1/0.05 = 20.  The err_t sequences are regenerated from
% the committed src/forfeit.py, whose adversarial regime is fully deterministic and consumes no
% randomness, and the regeneration was checked against the frozen
% results/forfeit-20260820T063132Z-83747c45.json: miscoverage and max|E| agree to the last stored
% digit at T = 10^4 and 10^5 for tau = 0.5, 0.9 and 1.5.
%   tau = 1.5 past the edge misses every round (miscoverage 1.000000), so M_t(+1) = 1.9^t and
%     M_t(-1) = 0.1^t.  Mixture: 1.000, 1.810, 3.430, 6.516, 12.381, 23.523 at t = 1..6.  It
%     crosses 20 on round SIX, not the fifth.  The old "fifth" was M_t(+1) alone (13.032 then
%     24.761), i.e. the single-sided number the invalid post-hoc sign bought.  The mixture's
%     factor of two is the whole difference, and it costs exactly one round here.
%   Inside the edge the mixture peaks at 2.0259 (tau = 0.5, round 8) and 3.9230 (tau = 0.9,
%     round 23) and decays after, over the full 10^6-round run; printed rounded to 2.03 and 3.92.
%   "at any horizon" is not just measured, it is closed: log M_t(+1) = t log 0.9 + (E_t + 0.1t)
%     log(19/9) and log M_t(-1) = t log 1.1 - (E_t + 0.1t) log 11, so both are maximised over
%     paths at |E_t| = c h(t) + 1, Proposition 2's bound.  Feeding that bound in, the mixture
%     cannot reach 20 at any t >= 62 on ANY retention-side trajectory, and t <= 61 is inside the
%     measured range.  Hence "from round 62 on ... forbids rejection on every retained
%     trajectory", which is a stronger and checkable statement, not a softer one.
%   Mirror direction, for the record and not printed: on an all-cover trajectory (err_t = 0 every
%     round, the tau*- failure the theorem's mirror half produces) M_t(-1) = 1.1^t crosses 20 on
%     round 32 and the mixture on round 39.  The paragraph makes no mirror claim, so none is added.
% Working: research/S18/B-martingale-fix.json (research/ is gitignored).
% =========================================================================================
\section{The betting reading}
\label{app:evalue}

The boundary is stated in $|E_T|$, which is the net gain of a Skeptic who pays $\alpha$ each round
for $\mathrm{err}_t$; that is what gives it a betting reading. The reading
Section~\ref{sec:forfeit} points at is Ville's inequality applied to the standard
martingale $M_t(\lambda) = \prod_{i \le t}\big(1 + \lambda(\mathrm{err}_i - \alpha)\big)$, whose
logarithm depends on the path only through $t$ and $E_t$
\citep{shafer2019gtfpf,ramdas2023savi,vovk2021evalues}, under the imposed null that
$\mathrm{err}_t \mid \mathcal{F}_{t-1}$ is Bernoulli$(\alpha)$, with $\mathcal{F}_{t-1}$ the
history through round $t-1$. Ville's inequality needs each bet to be predictable, fixed by the
history before the round it is scored on, so the direction of failure cannot be read off the
realised path first. The diagnostic is therefore
the two-sided mixture $M_t = \tfrac{1}{2}M_t(+1) + \tfrac{1}{2}M_t(-1)$, which is a test
martingale for the same null because a convex combination of test martingales is one, and which
fixes no direction in advance; both signs lie in $[-1/(1-\alpha),\,1/\alpha]$, the range on which
$M_t(\lambda) \ge 0$ on every path, the magnitude $1$ is the same for every arm and both
directions, and the mixture costs a factor of two against a correctly guessed direction. So read,
an anytime-valid test of the deployed intervals' calibration convicts the $\tau = 1.5$ arm on its
sixth interval at the $5\%$ level, where $M_5 = 12.38$ and $M_6 = 23.52$ against the rejection
threshold $1/0.05 = 20$. Inside the edge the same test cannot reject at that level at any horizon:
it peaks at $2.03$ for $\tau = 0.5$ and $3.92$ for $\tau = 0.9$, and from round $62$ on
Proposition~2's bound on $|E_t|$ forbids rejection on every retained trajectory. $M_t$ is an
e-value for that imposed null alone; the accumulator itself is signed and is neither an e-value
nor an e-process, and no result in Section~\ref{sec:forfeit} assumes that null or is certified by
that test.


% =========================================================================================
% S15 SUB-SESSION D (2026-08-24): APPENDIX B "THE FOUR VOCABULARIES" NO LONGER EXISTS AS A
% SECTION, AND ITS \clearpage IS GONE.  Both changes are removals of dead structure.
%
% (1) THE SECTION.  After S13 sub-session B deleted Table 4 and folded its content into a
%     paragraph, "The four vocabularies" was a section heading over ONE paragraph of
%     related-work prose.  "Where this sits" opened by pointing at it ("Four vocabularies
%     name one quantity, and Appendix~B names all four; this appendix places this paper's
%     claim and its concessions among them") -- i.e. two sections, one topic, split only
%     because one of them used to be a table in the appendix and the other used to be a body
%     section exiled by the 4-page ceiling.  The paragraph now opens
%     Section~\ref{sec:related} in sections/related.tex, WORD FOR WORD, with every \citep key
%     and its locator intact and the ten-name list (docs/GATES.md G7.11's concession) intact.
%     \label{app:bridge} is retired with the section; its two live pointers, in
%     sections/setup.tex and in that section's own opening sentence, are repointed at
%     \ref{sec:related} -- see those files.
%
% (2) THE \clearpage.  S6 sub-session E added it so that this heading could not strand away
%     from "its only float", and priced it at zero because "appendix pages are outside
%     E-values' 4-page ceiling".  BOTH HALVES OF THAT ARE NOW FALSE: the float it protected
%     was deleted in S13, so it protected nothing, and TS-LIMITS counts appendix pages, so a
%     forced page break is not free.  MEASURED, not assumed: it was leaving Table~\ref{tab:stress}
%     alone on a page 69% white.  Removing it lets the remaining appendix material pack.
% =========================================================================================
